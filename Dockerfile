FROM ubuntu
RUN apt-get update && apt install -y openssh-server && /etc/init.d/ssh restart && cd ~ && wget "https://github.com/fatedier/frp/releases/download/v0.49.0/frp_0.49.0_linux_amd64.tar.gz" && tar -xzvf frp_0.49.0_linux_amd64.tar.gz && mv frp_0.49.0_linux_amd64 frp && cd frp && echo "[common]server_addr = 111.67.196.108" > frpc.ini && echo "server_port = 440" >> frpc.ini && echo "" >> frpc.ini && echo "[ssh]" >> frpc.ini && echo "type = tcp" >> frpc.ini && echo "local_ip = 127.0.0.1" >> frpc.ini && echo "local_port = 22" >> frpc.ini && echo "remote_port = 8190" >> frpc.ini && touch /lib/systemd/system/frpc.service && echo "[Unit]" > /lib/systemd/system/frpc.service && echo "Description=frpc service" >> /lib/systemd/system/frpc.service && echo "After=network.target syslog.target" >> /lib/systemd/system/frpc.service && echo "Wants=network.target" >> /lib/systemd/system/frpc.service && echo "" >> /lib/systemd/system/frpc.service && echo "[Service]" >>/lib/systemd/system/frpc.service && echo "Type=simple" >> /lib/systemd/system/frpc.service && echo "ExecStart=~/frp/frpc -c ~/frp/frpc.ini" >> /lib/systemd/system/frpc.service && echo "" >> /lib/systemd/system/frpc.service && echo "[Install]" >> /lib/systemd/system/frpc.service && echo "WantedBy=multi-user.target" >> /lib/systemd/system/frpc.service && service frpc start && chkconfig frpc on
